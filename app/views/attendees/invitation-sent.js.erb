
var emailModal = $('#email-invite-modal').remodal();
        emailModal.close();

$.notify({
    text: "Invite sent!",
    image: "http://i.giphy.com/JIX9t2j0ZTN9S.gif"
});

//setTimeout(window.location = '/dashboard/index', 2000);