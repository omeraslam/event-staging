<% title @user.email + 'events' %>
<% meta_description '' %>
<% @band = true %>

<style type="text/css">
  html, body {
    height: 100%;
  }
  .wrapper {
    background-image: url(../assets/brots.jpg);
    width: 100%;
    height: 100%;
    background-size: cover;
  }

  .wrapper {
    color: white;
  }
  h1 {
    text-align: center;
  }

  ul.upcoming-events {
  
  }

  ul.upcoming-events li {
    display: inline-block;
    width: 200px;
    height: 200px;
    background: #222;
    margin-top: 40px;
    margin-right: 20px;
  }
   ul.upcoming-events li img {
    width: 100px;
   }
</style>

<div class="wrapper">

<div class="container">
  
<h1><%= @band ? 'Blazing Rays of the Sun':@user.email %></h1>

  <h2>Join us for upcoming events</h2>
  <ul class="upcoming-events">
<% @events.each do |event| %>
<li class="event-tile">
<div style="background:url(<%= background_checker(event, false)%>); width: 100%; height: 100%; background-size: cover; background-position: 0 0;">
</div>
 <%= event.name %><br/>
 <% if !event.date_start.empty? %>
  <%= event.date_start.to_date.strftime("%B %d, %Y") %>
 <% end %>
 </li>
<% end %>
</ul>

<h3>#BROTS</h3>

<a href="https://api.instagram.com/oauth/authorize/?client_id=0a4ce555797642009a4728615f6b8b08&redirect_uri=http://localhost:3000/events/index&response_type=token&scope=public_content">Connect your instagram</a>


<script type="text/javascript">
  //https://api.instagram.com/v1/tags/{tag-name}/media/recent?access_token=ACCESS-TOKEN
  $(document).ready(function() {

    // $.ajax({

    // url: 'https://www.googleapis.com/moderator/v1/series?key='+key,
    // data: myData,
    // type: 'GET',
    // crossDomain: true,
    // dataType: 'jsonp',

    alert(window.location.href.split('#access_token=')[1]);

    $.ajax({
      url:'https://api.instagram.com/v1/tags/hashtaglunchbagla/media/recent?access_token='+window.location.href.split('#access_token=')[1], 
      dataType: 'jsonp', 
      type: 'GET',
      success: function(data) {
      console.log(JSON.stringify(data.data));
     // var img = data.data[0].images.standard_resolution.url;
        // var imagehtml = '<img src="'+img+'" />';
        // $('.container').append(imagehtml);
    }
    });
  })
</script>




</div>

</div>


