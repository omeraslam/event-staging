<div class="container dashboard">



  <h1>Manage your events</h1>
  
  <div class="events-list">
	  <% @events.each do |event| %>
	  <div class="event">
	  	<div class="image" style="background-image:url(<%= image_path(event.background_img) %>);"> </div>
	  	<div class="details"> 
	  	<%= link_to event.name, user_event_path(@user, event), :class => 'title'  %> 
	  		<div class="time"><%= event.event_time.strftime("%B %d at %I:%M %p") %> </div>
	  		<div class="location" style="display: none;"><%= event.location %> </div>
	  		<div class="registrants"> 
<!--	  			<%= link_to 'Invite guests', new_attendee_path(:event => event.id) %>-->
	  		 <i class="icon icon-users"> </i>	<%= link_to 'View Guest List', attendees_path(:event => event.id)%>	  		
	  		</div>
	  	<div class="actions"> 
	  	<!--	  			<%= link_to 'Invite guests', new_attendee_path(:event => event.id) %>-->
	  		  			<%= link_to 'Edit event', edit_user_event_path(@user, event) %>
	  		  		<%= link_to 'Delete event', user_event_path(@user, event), method: :delete, data: { confirm: 'Are you sure?' } %>
	  		  		
	  		  		</div>
	  	
	  	
	  	
	  	</div>
	  

		</div>	  
		<% end %>
  </div>
  
  
<!--  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Time</th>
        <th>Location</th>
        <th>Background</th>
        <th>Guest List</th>
        <th>Invite guests</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @events.each do |event| %>
      <tr>
        <td><%= link_to event.name, user_event_path(@user, event) %></td>
        <td><%= event.description %></td>
        <td><%= event.event_time.strftime("%B %d at %I:%M %p") %></td>
        <td><%= event.location %></td>
        <td><%= image_tag(event.background_img) %></td>
        <td><%= link_to 'Invite guests', new_attendee_path(:event => event.id) %></td>
        <td><%= link_to 'Show Guest List', attendees_path(:event => event.id)%></td>
        <td><%= link_to 'Edit', edit_user_event_path(@user, event) %></td>
        <td><%= link_to 'Destroy', user_event_path(@user, event), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>-->
  </table>


  <%= link_to 'New Event', '/event-create', :class => 'btn btn-primary'  %>





</div>

