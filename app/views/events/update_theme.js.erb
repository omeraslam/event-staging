<% if @event.update(params.require(:event).permit(:background_img)) %>
	$('.upload').find('img').remove();
	$('.upload').css('background-image', 'url(\'<%= @event.background_img %>\')');
	
<% else %>
	alert("Failed to upload image");
<% end %>