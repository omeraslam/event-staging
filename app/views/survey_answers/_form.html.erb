<%= form_for(@survey_answer) do |f| %>
  <% if @survey_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_answer.errors.count, "error") %> prohibited this survey_answer from being saved:</h2>

      <ul>
      <% @survey_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer_text %><br>
    <%= f.text_area :answer_text %>
  </div>
  <div class="field">
    <%= f.label :attendee_id %><br>
    <%= f.number_field :attendee_id %>
  </div>
  <div class="field">
    <%= f.label :survey_question_id %><br>
    <%= f.number_field :survey_question_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
